version: 0.2

phases:
  pre_build:
    commands:
      - make prepare

  build:
    commands:
      - make clean build package

  post_build:
    commands:
      - make publish

artifacts:
  files:
    - appspec.yml
    - bin/*
    - scripts/*
  discard-paths: no